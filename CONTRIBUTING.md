# Guía de Contribución

1. **Ramificaciones:** Usa `feature/nombre-de-la-tarea` o `fix/nombre-del-bug`.
2. **Pull Requests:** Toda PR debe pasar por el pipeline de QA (`ruff` + `black` + `pytest`).
3. **Seguridad:** Nunca incluyas archivos `.env`, `.json` de credenciales o claves privadas.
